<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Prescriptions</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/html2pdf.js/0.10.1/html2pdf.bundle.min.js"></script>
</head>

<body>
    <div class="d-flex" id="wrapper">
        <%- include('../partials/sidebar') %>
            <div id="page-content-wrapper" class="main-gradient-bg">
                <nav class="navbar navbar-expand-lg navbar-light bg-transparent py-4 px-4">
                    <div class="d-flex align-items-center">
                        <i class="fas fa-align-left primary-text fs-4 me-3" id="menu-toggle"></i>
                        <h2 class="fs-2 m-0 text-dark fw-bold">Prescriptions</h2>
                    </div>
                </nav>
                <div class="container-fluid px-4 mb-5">
                    <div class="row g-4">
                        <div class="col-12">
                            <div class="dash-card p-4 shadow-sm bg-white rounded-4 table-responsive h-100">
                                <% if(prescriptions && prescriptions.length> 0) { %>
                                    <table class="table table-hover align-middle mb-0">
                                        <thead class="bg-light">
                                            <tr>
                                                <th
                                                    class="py-3 ps-3 rounded-start text-muted small text-uppercase fw-bold">
                                                    Doctor</th>
                                                <th class="py-3 text-muted small text-uppercase fw-bold">Date</th>
                                                <th class="py-3 text-muted small text-uppercase fw-bold"
                                                    style="max-width: 300px;">Diagnosis / Prescription</th>
                                                <th
                                                    class="py-3 pe-3 rounded-end text-end text-muted small text-uppercase fw-bold">
                                                    Action</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <% prescriptions.forEach(function(pres, index) { %>
                                                <tr>
                                                    <td class="ps-3">
                                                        <div class="d-flex align-items-center">
                                                            <img src="<%= getProfilePic({ profilePic: pres.doctorPic, role: 'doctor' }) %>"
                                                                class="rounded-circle me-3 shadow-sm" width="40"
                                                                height="40" style="object-fit: cover;"
                                                                onerror="this.onerror=null; this.src='/images/doctor-default.png';">
                                                            <div>
                                                                <div class="fw-bold text-dark text-uppercase">Dr. <%=
                                                                        pres.doctorName %>
                                                                </div>
                                                                <small class="text-muted">
                                                                    <%= pres.doctorSpeciality %>
                                                                </small>
                                                            </div>
                                                        </div>
                                                    </td>
                                                    <td>
                                                        <div class="fw-bold small text-dark">
                                                            <%= new Date(pres.issuedAt || pres.date).toDateString() %>
                                                        </div>
                                                    </td>
                                                    <td style="max-width: 300px;">
                                                        <span class="d-inline-block text-truncate w-100 text-muted"
                                                            title="<%= pres.advice || 'No details' %>">
                                                            <%= pres.advice || 'No details provided.' %>
                                                        </span>
                                                    </td>
                                                    <td class="text-end pe-3">
                                                        <button class="btn btn-sm btn-outline-primary rounded-pill px-3"
                                                            data-bs-toggle="modal"
                                                            data-bs-target="#prescriptionModal<%= index %>">
                                                            <i class="fas fa-eye me-1"></i> View Details
                                                        </button>
                                                    </td>
                                                </tr>
                                                <% }) %>
                                        </tbody>
                                    </table>
                                    <% } else { %>
                                        <div class="text-center py-5">
                                            <i class="fas fa-file-prescription fs-1 text-muted opacity-25 mb-3"></i>
                                            <p class="text-muted fw-bold">No prescriptions found</p>
                                        </div>
                                        <% } %>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
    </div>

    <% if(prescriptions && prescriptions.length> 0) { %>
        <% prescriptions.forEach(function(pres, index) { %>
            <!-- Floating Modal for Details (Keep existing modal logic) -->
            <div class="modal fade" id="prescriptionModal<%= index %>" tabindex="-1" aria-hidden="true"
                style="z-index: 10000;">
                <div class="modal-dialog modal-dialog-centered modal-lg">
                    <div class="modal-content border-0 shadow rounded-4">
                        <div class="modal-header border-0 pb-0">
                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                        </div>
                        <div class="modal-body p-5" id="pdfContent<%= index %>">
                            <div class="text-center mb-4">
                                <img src="/images/logo.png" height="50" class="mb-2"
                                    onerror="this.style.display='none'">
                                <h3 class="fw-bold text-primary">MediConnect Prescription</h3>
                                <p class="text-muted small">Generated on <%= new Date().toDateString() %>
                                </p>
                            </div>

                            <div class="row mb-5">
                                <div class="col-md-6">
                                    <div class="d-flex align-items-center">
                                        <img src="<%= getProfilePic({ profilePic: pres.doctorPic, role: 'doctor' }) %>"
                                            class="rounded-circle me-3 border" width="60" height="60"
                                            style="object-fit: cover;"
                                            onerror="this.onerror=null; this.src='/images/doctor-default.png';">
                                        <div>
                                            <h5 class="fw-bold text-dark mb-0">Dr. <%= pres.doctorName %>
                                            </h5>
                                            <p class="text-muted mb-0">
                                                <%= pres.doctorSpeciality %>
                                            </p>
                                        </div>
                                    </div>
                                </div>
                                <div class="col-md-6 text-md-end mt-3 mt-md-0">
                                    <h6 class="fw-bold text-muted text-uppercase">Prescribed For</h6>
                                    <h5 class="fw-bold text-dark mb-0">
                                        <%= user.fullName %>
                                    </h5>
                                    <p class="text-muted mb-0">
                                        <%= new Date(pres.issuedAt || pres.date).toDateString() %>
                                    </p>
                                </div>
                            </div>

                            <div class="bg-light p-4 rounded-3 mb-4">
                                <h6 class="fw-bold text-primary text-uppercase mb-3"><i
                                        class="fas fa-stethoscope me-2"></i>Diagnosis / Prescription</h6>
                                <p class="text-dark mb-0"
                                    style="white-space: pre-wrap; font-size: 1.1rem; line-height: 1.6;">
                                    <%= pres.advice %>
                                </p>
                            </div>

                            <div class="text-center mt-5 pt-3 border-top">
                                <small class="text-muted">Digital Prescription • MediConnect Platform</small>
                            </div>
                        </div>
                        <div class="modal-footer border-0 justify-content-center pb-4">
                            <button type="button" class="btn btn-outline-secondary rounded-pill px-4"
                                data-bs-dismiss="modal">Close</button>
                            <button type="button" class="btn btn-success rounded-pill px-4"
                                onclick="downloadPDF('pdfContent<%= index %>', 'Prescription_<%= pres.doctorName %>')">
                                <i class="fas fa-file-download me-2"></i>Download as PDF
                            </button>
                        </div>
                    </div>
                </div>
            </div>
            <% }) %>
                <% } %>

                    <%- include('../partials/footer') %>
                        <script>
                            var el = document.getElementById("wrapper");
                            var toggleButton = document.getElementById("menu-toggle");
                            if (toggleButton) {
                                toggleButton.onclick = function () {
                                    el.classList.toggle("toggled");
                                };
                            }

                            function downloadPDF(elementId, filename) {
                                const element = document.getElementById(elementId);
                                const opt = {
                                    margin: 0.5,
                                    filename: filename + '.pdf',
                                    image: { type: 'jpeg', quality: 0.98 },
                                    html2canvas: { scale: 2 },
                                    jsPDF: { unit: 'in', format: 'letter', orientation: 'portrait' }
                                };
                                html2pdf().set(opt).from(element).save();
                            }
                        </script>
</body>

</html>